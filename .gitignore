/.harness/
/target
lcov.info

# Mutation-run output: 94 MB of logs and per-mutant diffs, regenerated by
# `cargo mutants`. Committed by accident via `git add -A`.
/mutants.out/
/mutants.out.old/

# Mutation sweep logs, written by the container into the bind mount.
mutants-container.log
mutants.out/
