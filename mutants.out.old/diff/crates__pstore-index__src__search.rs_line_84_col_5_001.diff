--- crates/pstore-index/src/search.rs
+++ replace dist2 -> f32 with 1.0
@@ -76,10 +76,10 @@
         .copied()
         .collect();
     out.sort_unstable();
     out.dedup();
     out
 }
 
 fn dist2(a: &[f32], b: &[f32]) -> f32 {
-    a.iter().zip(b).map(|(x, y)| (x - y) * (x - y)).sum()
+    1.0 /* ~ changed by cargo-mutants ~ */
 }
