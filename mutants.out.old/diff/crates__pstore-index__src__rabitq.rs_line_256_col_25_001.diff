--- crates/pstore-index/src/rabitq.rs
+++ replace / with * in Quantizer::rotate
@@ -248,17 +248,17 @@
                 for (x, y) in lo.iter_mut().zip(hi.iter_mut()) {
                     let (u, w) = (*x, *y);
                     *x = u + w;
                     *y = u - w;
                 }
             }
             h *= 2;
         }
-        let scale = 1.0 / (self.padded as f32).sqrt();
+        let scale = 1.0 * /* ~ changed by cargo-mutants ~ */ (self.padded as f32).sqrt();
         for x in a.iter_mut() {
             *x *= scale;
         }
         a
     }
 
     /// Encodes a vector as its residual from `centroid`.
     ///
