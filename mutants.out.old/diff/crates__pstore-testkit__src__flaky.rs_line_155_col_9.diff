--- crates/pstore-testkit/src/flaky.rs
+++ replace <impl BlobStore for Flaky>::capabilities -> &Capabilities with Box::leak(Box::new(Default::default()))
@@ -147,17 +147,17 @@
         }
         Ok(())
     }
 }
 
 #[async_trait::async_trait]
 impl BlobStore for Flaky {
     fn capabilities(&self) -> &Capabilities {
-        self.inner.capabilities()
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
     async fn get(&self, key: &Key) -> Result<Bytes, BlobError> {
         self.read_gate()?;
         self.inner.get(key).await
     }
     async fn get_range(&self, key: &Key, range: std::ops::Range<u64>) -> Result<Bytes, BlobError> {
         self.read_gate()?;
         self.inner.get_range(key, range).await
