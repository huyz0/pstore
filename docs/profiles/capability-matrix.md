# Capability matrix

Generated by `scripts/conformance.sh` on 2026-09-12. **Do not edit by hand** — `scripts/conformance.sh --check` compares a fresh run against this file.

⚠️ **These are emulators, and emulators test plumbing** (D-99). `Supported` here means this emulator answered ten probes correctly. It says nothing about S3, GCS or Azure, whose profiles need real accounts — M0b.

⚠️ **Two of six fields are measured.** `cas` and `create_if_absent` come from the probes. `backend` is a label; `delete_is_free` is a billing fact no probe can see; `max_batch_delete` would need a search that is its own probe; `coalesce_gap` is `G*`, which is OQ-2 and blocked on real clouds. Those four are **declared**.

## minio

| probe | outcome |
|---|---|
| `create_if_absent` | Supported |
| `compare_and_swap` | Supported |
| `aba_resistance` | Divergent — a stale tag was accepted after the content returned to its first value: tags are content-derived, so the ABA hazard is live |
| `ranged_read` | Supported |
| `suffix_read` | Supported |
| `range_past_end_is_an_error` | Supported |
| `coalesced_read` | Supported |
| `get_tag` | Supported |
| `missing_key_is_an_error` | Supported |
| `batch_delete` | Supported |

| field | source | value |
|---|---|---|
| `cas` | measured | Supported |
| `create_if_absent` | measured | Supported |
| `backend` | declared | `minio` |
| `delete_is_free` | declared | false |
| `max_batch_delete` | declared | 1000 |
| `coalesce_gap` | declared | 1048576 |

Admits durable writes: **true**

## azurite

| probe | outcome |
|---|---|
| `create_if_absent` | Supported |
| `compare_and_swap` | Supported |
| `aba_resistance` | Supported |
| `ranged_read` | Supported |
| `suffix_read` | Divergent — suffix read failed: blob store error: Operation not supported: Azure does not support suffix range requests |
| `range_past_end_is_an_error` | Supported |
| `coalesced_read` | Supported |
| `get_tag` | Supported |
| `missing_key_is_an_error` | Supported |
| `batch_delete` | Supported |

| field | source | value |
|---|---|---|
| `cas` | measured | Supported |
| `create_if_absent` | measured | Supported |
| `backend` | declared | `azurite` |
| `delete_is_free` | declared | false |
| `max_batch_delete` | declared | 1000 |
| `coalesce_gap` | declared | 1048576 |

Admits durable writes: **true**

## fake-gcs-server

**UNREACHABLE** — blob store error: Generic GCS error: Error performing PUT http://localhost:4443/pstore/conformance%2Ffake%2Dgcs%2Dserver%2Fcanary in 1.571967ms - Server returned non-2xx status code: 400 Bad Request: {"error":{"code":400,"message":"invalid uploadType","errors":[{"domain":"global","reason":"invalid","message":"invalid uploadType"}]}}


No probe ran. This is recorded as an absence, never as a pass.

⚠️ **But the question was answered another way, and the answer is a no.** `scripts/conformance.sh --gcs-precondition` asks over the JSON upload path — the one route this client cannot take — and `ifGenerationMatch` is **accepted and ignored**: create-if-absent against an existing object returns 200 and overwrites, as does a compare-and-swap on a stale generation. Measured identically on 1.52 and 1.55.

⚠️ That is the worst shape a precondition can have — MinIO's wildcard had it (C-13) — because a CAS built on it reports success and loses the write. **Do not use this emulator as a CAS target.**
