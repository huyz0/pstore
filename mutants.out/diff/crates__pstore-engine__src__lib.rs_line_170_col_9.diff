--- crates/pstore-engine/src/lib.rs
+++ replace Engine<S>::mem -> std::sync::MutexGuard<'_, Memtable> with MutexGuard::new()
@@ -162,19 +162,17 @@
             mem: Mutex::new(Memtable::default()),
             seq: Mutex::new(Seq::ZERO),
             flushing: tokio::sync::Mutex::new(()),
             committed: Mutex::new(Epoch::ZERO),
         }
     }
 
     fn mem(&self) -> std::sync::MutexGuard<'_, Memtable> {
-        self.mem
-            .lock()
-            .unwrap_or_else(std::sync::PoisonError::into_inner)
+        MutexGuard::new() /* ~ changed by cargo-mutants ~ */
     }
 
     /// The last epoch this engine committed.
     #[must_use]
     pub fn epoch(&self) -> Epoch {
         *self
             .committed
             .lock()
