--- crates/pstore-index/src/sq8.rs
+++ replace * with / in estimate
@@ -122,10 +122,10 @@
 #[must_use]
 pub fn estimate(c: &Code, query: &[f32]) -> f32 {
     let mut sum_q = 0.0f32;
     let mut acc = 0.0f32;
     for (b, q) in c.codes.iter().zip(query) {
         sum_q += *q;
         acc += f32::from(*b) * *q;
     }
-    c.offset * sum_q + c.step * acc
+    c.offset / /* ~ changed by cargo-mutants ~ */ sum_q + c.step * acc
 }
