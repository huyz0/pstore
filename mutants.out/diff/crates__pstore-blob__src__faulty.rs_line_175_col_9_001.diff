--- crates/pstore-blob/src/faulty.rs
+++ replace <impl crate::BlobStore for Faulty<S>>::get_tag -> Option<pstore_types::CasTag> with Some(Default::default())
@@ -167,17 +167,17 @@
     async fn get_suffix(&self, key: &Key, n: u64) -> Result<Bytes, BlobError> {
         match self.read_fault() {
             Some(e) => Err(e),
             None => self.inner.get_suffix(key, n).await,
         }
     }
 
     async fn get_tag(&self, key: &Key) -> Option<pstore_types::CasTag> {
-        self.inner.get_tag(key).await
+        Some(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     async fn head(&self, key: &Key) -> Result<u64, BlobError> {
         match self.read_fault() {
             Some(e) => Err(e),
             None => self.inner.head(key).await,
         }
     }
